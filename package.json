{
    "name": "chitkara-diaries-root",
    "version": "1.0.0",
    "description": "Root script for deployment",
    "scripts": {
        "start": "node server/server.js",
        "build": "npm install --prefix client && npm run build --prefix client",
        "install-all": "npm install --prefix client && npm install --prefix server",
        "deploy": "npm install && npm run install-all && npm run build && npm start"
    },
    "dependencies": {
        "express": "^5.1.0",
        "cors": "^2.8.5",
        "dotenv": "^16.0.0"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}